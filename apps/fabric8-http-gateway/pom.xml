<!--

     Copyright 2005-2014 Red Hat, Inc.

     Red Hat licenses this file to you under the Apache License, version
     2.0 (the "License"); you may not use this file except in compliance
     with the License.  You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or
     implied.  See the License for the specific language governing
     permissions and limitations under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.fabric8</groupId>
    <artifactId>apps</artifactId>
    <version>2.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>fabric8-http-gateway</artifactId>
  <packaging>jar</packaging>

  <name>Fabric8 :: HTTP Gateway</name>
  <description>Fabric8 HTTP Gateway</description>

  <properties>
    <guava.version>15.0</guava.version>

    <!-- fabric8 configuration -->
    <docker.dataImage>${env.DOCKER_REGISTRY}/fabric8/${project.artifactId}:${project.version}</docker.dataImage>
    <docker.port.container.gateway>${httpgateway.port}</docker.port.container.gateway>
    <docker.port.host.gateway>${httpgateway.port}</docker.port.host.gateway>
    <docker.port.host.jolokia>9802</docker.port.host.jolokia>
    <fabric8.kubernetes.name>fabric8HTTPGateway</fabric8.kubernetes.name>
    <fabric8.env.AMQ_PORT>${activemq.container.port}</fabric8.env.AMQ_PORT>
    <fabric8.label.container>java</fabric8.label.container>
    <fabric8.label.group>defaultGatewayGroup</fabric8.label.group>
    <fabric8.iconRef>icons/activemq.svg</fabric8.iconRef>

    <!-- jube properties -->
    <docker.env.MAIN>org.jboss.weld.environment.se.StartMain</docker.env.MAIN>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.jboss.weld.se</groupId>
      <artifactId>weld-se</artifactId>
    </dependency>
    <dependency>
      <groupId>io.fabric8</groupId>
      <artifactId>gateway-fabric-support</artifactId>
      <version>2.0.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <forkMode>perTest</forkMode>
        </configuration>
      </plugin>

      <plugin>
        <groupId>io.fabric8</groupId>
        <artifactId>fabric8-maven-plugin</artifactId>
        <version>${project.version}</version>
        <executions>
          <execution>
            <id>json</id>
            <goals>
              <goal>json</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.jolokia</groupId>
        <artifactId>docker-maven-plugin</artifactId>
        <version>${docker.maven.plugin.version}</version>
        <configuration>
          <image>fabric8/java</image>
          <mergeData>true</mergeData>
          <assemblyDescriptorRef>artifact-with-dependencies</assemblyDescriptorRef>
          <env>
            <MAIN>org.jboss.weld.environment.se.StartMain</MAIN>
            <HTTPGATEWAY_PORT>${httpgateway.port}</HTTPGATEWAY_PORT>
          </env>
          <ports>
            <port>${httpgateway.port}</port>
            <port>8778</port>
          </ports>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>${exec-maven-plugin.version}</version>
        <configuration>
          <mainClass>org.jboss.weld.environment.se.StartMain</mainClass>
          <includePluginDependencies>false</includePluginDependencies>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>docker-build</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jolokia</groupId>
            <artifactId>docker-maven-plugin</artifactId>
            <version>${docker.maven.plugin.version}</version>
            <executions>
              <execution>
                <goals>
                  <goal>build</goal>
                </goals>
                <phase>package</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
